'use strict';

var expect = require("chai").expect;
var headerParser = require(__dirname + "/../lib/header_parser");

describe('headerParser', fuction(){
	it('should be passed header variable', function(){
		expect(typeof(header)).to.eql(typeof({}));
	});
});

